"""Agent (RL algorithm) configuration files for RobotArm tasks.

    reach_ppo_cfg.yaml -- placeholder PPO hyperparameters for RobotArm-Reach-v0
                          (scaffold only; no trainer is wired up yet)

Referenced by the task registration as ``agent_cfg_entry_point`` so a future RL
runner can discover it via the gym registry.
"""
